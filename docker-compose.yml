version: '3.8'
services:
  web:
    build: .
    ports: ['8000:8000']
    env_file: .env
    volumes: ['.:/app']
  db:
    image: postgres:15
    environment:
      POSTGRES_DB: jblb
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: secret
    ports: ['5432:5432']
